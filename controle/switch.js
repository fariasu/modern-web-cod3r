const imprimirResultado = function (nota) {
    switch (Math.floor(nota)) {
        case 10:    // se for 10 ou 9 ele executa as 2 linhas
        case 9:
            console.log('Quadro de Honra')
    }
}